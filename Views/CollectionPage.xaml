<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SystemKolekcjonerstwo.Views.CollectionPage"
             Title="CollectionPage">
    <VerticalStackLayout>
        <HorizontalStackLayout WidthRequest="100">
            <Button Text="Dodaj element" Clicked="AddElement_Clicked" HorizontalOptions="Center" VerticalOptions="Center"/>
            <Button Text="Usuń element" Clicked="RemoveElement_Clicked" HorizontalOptions="Center" VerticalOptions="Center"/>
            <Button Text="Edytuj element" Clicked="EditElement_Clicked" HorizontalOptions="Center" VerticalOptions="Center"/>
        </HorizontalStackLayout>
        <CollectionView x:Name="elementsView" SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout>
                        <Label Text="{Binding Name}"></Label>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>